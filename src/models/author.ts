class Author {
    id: number;
    first_name: string;
    surname: string;

    constructor(id: number, first_name: string, surname: string) {
        this.id = id;
        this.first_name = first_name;
        this.surname = surname;
    }
}
export default Author;
